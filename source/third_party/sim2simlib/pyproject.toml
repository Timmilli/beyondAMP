[project]
name = "sim2simlib"
version = "0.1.0"
description = "sim2sim utils"
license = "BSD-3-Clause"
requires-python = ">=3.10"
keywords = ["extension", "isaacLab"]
dependencies = [
    "numpy",
    "mujoco",
    "torch",
    "rich",
]
